A labeled text input. The label wraps the input; pass any native input attribute through.

```jsx
<Field label="Email" name="email" type="email" autoComplete="username" required />
<Field label="Notice ID" name="noticeId" readOnly value="SP4701-25-R-0042" hint="From SAM.gov" />
```

`hint` renders muted helper text below the input.
