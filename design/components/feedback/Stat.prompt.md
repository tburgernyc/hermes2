A headline metric for the operator dashboard's stat grid.

```jsx
<Stat label="Awaiting sourcing decision" value={4} />
<Stat label="Deadlines within 72h" value={2} tone="warn" />
```

`tone="warn"` recolors the value amber to flag something needing attention.
