The BurgerGov monogram + wordmark. Use in app chrome (auth cards, nav, token pages).

```jsx
<Brand />
<Brand size="lg" />
```

`size`: `sm`, `md` (default), `lg`. The full app-icon PNG (`assets/burgergov-logo.png`) is the marketing/favicon mark; this inline lockup is for in-app chrome.
